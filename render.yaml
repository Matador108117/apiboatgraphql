services:
  - type: web
    name: my-boat-graphql
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn boatGraphql.wsgi:application"
    branch: release
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: boatdb
          property: connectionString

